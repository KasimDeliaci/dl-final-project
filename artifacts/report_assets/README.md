# Report Assets

Small report-ready generated assets can be staged here during analysis.

Large intermediate artifacts should remain outside Git. Final copied figures/tables may later be moved into `reports/` if needed for reproducible builds.

