backbone:
  id: vit_b16
  display_name: Vanilla ViT
  required_by_assignment: true
  family: vision_transformer
  library: timm
  model_id: vit_base_patch16_224.augreg_in21k_ft_in1k
  pretrained_source: ImageNet-21k pretraining with ImageNet-1k fine-tuning via timm pretrained cfg
  input_size: 224
  feature_extraction_point: classifier-free CLS token representation using num_classes=0 and global_pool=token
  feature_dimension: 768
  feature_source: frozen
  fine_tuning_policy:
    unfreeze_blocks: pending
    learning_rate: pending
  notes:
    - Sprint 2 uses CLS token features as the canonical single-backbone representation.
    - Fine-tuning policy remains pending until Sprint 4.
