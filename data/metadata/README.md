# Metadata

HAM10000 metadata files belong here.

Expected initial file after dataset setup:

```text
HAM10000_metadata.csv
```

Metadata source and normalization decisions must be documented in `docs/DATASET_AUDIT.md`.

