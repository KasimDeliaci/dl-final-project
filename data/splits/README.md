# Splits

Train/validation/test split CSV files will be stored here after Phase 0.

Expected files:

```text
train.csv
val.csv
test.csv
```

Split policy and leakage checks must be documented in `docs/DATASET_AUDIT.md`.

