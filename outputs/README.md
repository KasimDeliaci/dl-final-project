# Outputs

This directory mirrors the old project's generated-output convention.

Prefer `artifacts/` for the main experiment contract. Use `outputs/` only if a tool or notebook produces temporary outputs that later need to be promoted into `artifacts/`.

